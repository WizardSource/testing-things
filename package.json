{
  "name": "email-service",
  "version": "1.0.0",
  "description": "Email service with template management",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "vite",
    "build": "vite build",
    "setup": "chmod +x setup.sh && ./setup.sh",
    "backend": "nodemon src/index.js",
    "frontend": "cd client && npm run dev",
    "install-all": "npm install && cd client && npm install",
    "seed": "node src/db/seeds/seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "pg": "^8.11.0",
    "postmark": "^3.0.18"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
